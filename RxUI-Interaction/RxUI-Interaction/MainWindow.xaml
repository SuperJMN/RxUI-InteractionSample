<Window x:Class="RxUI_Interaction.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RxUI_Interaction"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" d:DataContext="{d:DesignInstance local:MainViewModel}">

    <Grid>
        <GroupBox Header="Incoming toys">
            <Grid>
                <DataGrid ItemsSource="{Binding Toys}" CanUserAddRows="False" IsReadOnly="True"
                          HorizontalContentAlignment="Stretch" AutoGenerateColumns="True" />
                <Border x:Name="brokenToyPanel" Background="Red" VerticalAlignment="Bottom" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <Label x:Name="fixItLabel">A toy is broken, can you fix it?</Label>
                        <Button x:Name="fixItButton">FIX IT!</Button>
                    </StackPanel>
                </Border>
            </Grid>
        </GroupBox>
    </Grid>
</Window>































































































































































































































































